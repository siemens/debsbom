{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/siemens/debsbom/refs/heads/main/src/debsbom/schema/schema-filter-exclude.json",
  "title": "Source Package Exclusion",
  "description": "A source package to exclude from the SBOM, identified by its exact name and version.",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the source package.",
      "pattern": "^[a-z0-9][a-z0-9+.-]+$"
    },
    "version": {
      "type": "string",
      "description": "The Debian version of the source package.",
      "minLength": 1
    }
  },
  "required": [
    "name",
    "version"
  ],
  "additionalProperties": false
}
